---
description: 企业级 REST/API 接口规范（RESTful、统一响应、分页、版本、文档）
globs: **/*Controller.java,**/domain/dto/**/*.java,**/domain/vo/**/*.java
alwaysApply: false
---

# 企业级 Java API 接口规范

本规范对齐阿里、字节、腾讯、美团等大厂常见实践，适用于 Spring Boot 后端。本仓库已使用 `Result<T>`、`PageResult<T>`、`GlobalExceptionHandler` 等实现，与下列规范一致。

---

## 一、接口设计原则

### 1. RESTful 风格
- 接口必须使用 **资源 + HTTP 方法** 表达语义，禁止用 URL 表达动作。
- 示例：
  - `GET /api/users` — 列表
  - `GET /api/users/{id}` — 详情
  - `POST /api/users` — 新增
  - `PUT /api/users/{id}` — 全量更新
  - `DELETE /api/users/{id}` — 删除

### 2. URL 设计规范
- 使用 **名词**，使用 **复数**，使用 **小写**，多词用 **连字符 `-`** 分隔。
- 正确：`/api/user-profiles`、`/api/order-items`、`/api/user-roles`。
- 错误：`/api/getUserList`、`/api/userList`、`/api/get_user`。

---

## 二、统一响应结构

- 标准结构：`code`（业务/HTTP 状态码）、`message`（提示信息）、`data`（业务数据）。
- 本仓库使用 `Result<T>` 封装，成功：`Result.ok(data)`，失败：`Result.fail(code, message)`。
- 返回示例：`{ "code": 0, "message": "success", "data": {} }`。

---

## 三、状态码规范

- **2xx**：成功（如 200）。
- **4xx**：客户端错误（如 400 参数错误、401 未认证、403 无权限、404 不存在）。
- **5xx**：服务端错误（如 500 系统异常）。
- 业务码可与 HTTP 状态码分离，由 `Result.code` 表达，HTTP 状态码建议成功 200、业务异常也可 200 由 body 中 code 区分。

---

## 四、分页接口规范

- **请求**：查询参数统一放 query，如 `GET /api/users?pageNum=1&pageSize=20`（本仓库使用 `pageNum`/`pageSize`）。
- **返回**：`Result<PageResult<T>>`，其中 `PageResult` 包含 `list`、`total`、`pageNum`、`pageSize`、`pages`。
- 分页查询必须使用分页参数，禁止一次性拉全量列表。

---

## 五、查询参数规范

- 列表、筛选条件统一通过 **Query 参数** 传递，对应 DTO 如 `UserQuery`。
- 示例：`GET /api/users?name=tom&status=1&pageNum=1&pageSize=10`。
- Controller 使用 POJO 接收：`list(UserQuery query)` 或 `list(@Valid UserQuery query)`，避免一长串 `@RequestParam`。

---

## 六、POST / PUT 请求规范

- 请求体使用 **JSON**，通过 `@RequestBody` 绑定。
- 入参使用 **DTO**（如 `UserCreateDTO`、`UserUpdateDTO`），并加 `@Valid` 做参数校验。
- 示例：`POST /api/users`，Body：`{ "name": "Tom", "email": "tom@example.com" }`，方法签名：`create(@Valid @RequestBody UserCreateDTO dto)`。

---

## 七、DTO 规范

- **CreateDTO**：新增入参（如 `UserCreateDTO`）。
- **UpdateDTO**：更新入参（如 `UserUpdateDTO`）。
- **Query**：列表/查询条件（如 `UserQuery`），含分页字段。
- **VO / DTO**：返回给前端的视图或传输对象，仅暴露必要字段；与 Entity 分离，禁止用 Entity 直接作为请求/响应体。

---

## 八、接口版本控制

- 推荐路径版本：`/api/v1/users`、`/api/v2/users`。
- 或通过 Header：`Accept: application/vnd.company.v1+json`。
- 本仓库可采用 `@RequestMapping("/api/v1/xxx")` 在 Controller 层统一前缀。

---

## 九、统一异常处理

- 使用 `@RestControllerAdvice` + `@ExceptionHandler` 统一处理异常，返回 `Result` 结构。
- 业务异常使用 `BusinessException`，由全局处理器转换为 `Result.fail(code, message)`，不把堆栈直接返回前端。
- 本仓库已使用 `GlobalExceptionHandler`，新增业务异常时在 Handler 中增加对应处理即可。

---

## 十、参数校验规范

- 必须使用 `javax.validation`（或 `jakarta.validation`）：`@NotBlank`、`@NotNull`、`@Email`、`@Size`、`@Min`/`@Max` 等。
- DTO 字段上加校验注解，Controller 方法入参使用 `@Valid` 或 `@Validated`。
- 示例：`create(@Valid @RequestBody UserCreateDTO dto)`。

---

## 十一、接口文档规范

- 必须使用 **Swagger / OpenAPI**（如 SpringDoc）维护接口文档。
- Controller 与方法上使用 `@Tag`、`@Operation(summary = "xxx")`，DTO/VO 使用 `@Schema(description = "xxx")`。
- 保证文档与真实接口一致，随代码变更同步更新。

---

## 十二、接口与类命名规范

- **Controller**：`UserController`、`OrderController`，与资源对应。
- **Service**：`UserService`、`OrderService`；实现类 `UserServiceImpl`。
- **方法**：`list`（分页列表）、`getById` / `detail`（详情）、`create`、`update`、`delete`，动词表达清晰语义。

---

## 十三、日志规范

- 关键接口记录请求参数（脱敏后）、关键业务 ID；异常时记录错误信息与必要上下文。
- 使用 SLF4J 占位符：`log.info("create user, dto={}", dto);`，禁止 `System.out`。
- 不记录敏感信息（密码、完整 token 等）。

---

## 十四、接口安全规范

- 认证：**JWT** 或 **OAuth2**，Token 通过 Header 传递：`Authorization: Bearer <token>`。
- 鉴权：根据角色/权限控制接口访问，与本仓库 Security 配置一致。
- 敏感接口限流、防重放按需在网关或 Filter 中实现。

---

## 十五、接口幂等性

- 订单创建、支付、关键写操作等必须考虑幂等。
- 方式：客户端传 **idempotency-key**，或服务端用 **唯一业务 ID** 去重。
- 设计时明确幂等语义（同一 key 多次请求结果一致）。

---

## 十六、返回字段规范

- 字段命名：**camelCase**（与 Java 一致），若需前端使用 snake_case 可全局配置 Jackson。
- 时间格式统一：如 `yyyy-MM-dd HH:mm:ss` 或 ISO 8601。
- 布尔字段命名：`isXxx` 或 `hasXxx`，语义明确。
- 不返回与前端无关的内部字段（如密码、内部状态码未约定时）。

---

## 十七、路径规范总结

- `GET    /api/v1/users`        — 分页列表
- `GET    /api/v1/users/{id}`   — 详情
- `POST   /api/v1/users`        — 新增
- `PUT    /api/v1/users/{id}`   — 更新
- `DELETE /api/v1/users/{id}`   — 删除

---

## 十八、Controller 示例

```java
@RestController
@RequestMapping("/api/v1/users")
@Tag(name = "用户管理")
public class UserController {

    @GetMapping
    @Operation(summary = "分页查询用户")
    public Result<PageResult<UserVO>> list(@Valid UserQuery query) {
        return Result.ok(userService.page(query));
    }

    @GetMapping("/{id}")
    @Operation(summary = "用户详情")
    public Result<UserVO> detail(@PathVariable Long id) {
        return Result.ok(userService.getById(id));
    }

    @PostMapping
    @Operation(summary = "新增用户")
    public Result<Long> create(@Valid @RequestBody UserCreateDTO dto) {
        return Result.ok(userService.create(dto));
    }

    @PutMapping("/{id}")
    @Operation(summary = "更新用户")
    public Result<Void> update(@PathVariable Long id, @Valid @RequestBody UserUpdateDTO dto) {
        userService.update(id, dto);
        return Result.ok();
    }

    @DeleteMapping("/{id}")
    @Operation(summary = "删除用户")
    public Result<Void> delete(@PathVariable Long id) {
        userService.delete(id);
        return Result.ok();
    }
}
```
