---
description: 企业级 Java 基础规范（编码、命名、格式、注释、类结构）
globs: **/*.java
alwaysApply: false
---

# Java 基础编码与命名规范

## 文件与格式
- 文件编码 UTF-8，行结束符 LF，缩进 4 空格，单行最大 120 字符。
- 大括号不换行：`if (condition) { doSomething(); }`，禁止单行 if 无括号。
- 运算符两边有空格：`if (count > 10) { ... }`
- 单方法建议 < 80 行，单类 < 500 行，复杂逻辑必须拆分。

## 类内部顺序
1. 常量（static final）
2. 成员变量
3. 构造方法
4. public 方法
5. protected 方法
6. private 方法

## 命名
- **包**：全小写，如 `com.zioneer.robotqcsystem.service`
- **类**：PascalCase。Controller / Service / ServiceImpl / Entity / DTO / VO / Config 等后缀规范使用。
- **方法**：camelCase，动词开头。get/create/update/delete/list/validate 等前缀表达语义。
- **变量**：camelCase，必须表达业务含义，禁止 a、tmp、data1 等无意义命名。
- **常量**：全大写下划线，如 `MAX_TASK_COUNT`、`DEFAULT_LANGUAGE`。

## 注释

### 注释类型与用途
- **文档注释 `/** */`**：仅用于类、接口、方法、常量、重要成员变量，供 IDE 悬浮提示与 Javadoc 生成；**禁止**用 `// xxx` 充当类/方法级说明。
- **块注释 `/* */`**：方法内部多行说明时使用，与代码对齐，前后可留空行。
- **单行注释 `//`**：写在被注释语句**上方**另起一行，说明单行或短逻辑；禁止在行尾写长注释。

### 类与接口（强制）
- 所有类、接口必须使用 Javadoc，首句简明说明**职责或用途**，不重复类名（避免「该类用于…」）。
- 建议包含：`@author` 创建者、`@since` 版本或日期；若为接口，说明实现方注意事项或契约。

### 方法（强制）
- 所有 **public / protected** 方法、**抽象方法及接口方法** 必须有 Javadoc。
- 首句：方法**做什么、在什么场景下使用**，不重复方法名。
- 必须按需书写：`@param 参数名 说明`（含取值范围、是否可空）、`@return 返回值含义`、`@throws 异常类型 何时抛出`。
- 若有业务约束、副作用、线程或性能注意点，在注释末尾补充说明。

### 常量与枚举
- 类内 **public static final** 常量应有 JavaDoc 或至少一行说明含义与取值用途。
- **枚举**：每个枚举常量必须有注释，说明在业务中的含义与使用场景。

### 行内与块注释（推荐）
- 用于解释**「为什么」**（业务原因、边界处理、历史兼容），而非复述**「做什么」**（代码已能表达）。
- 复杂分支、魔法数字、非显然的算法步骤，应加简短注释；与代码同缩进，保持精简。

### 语言与维护
- 注释语言：**优先中文**，表述准确；专有名词、技术术语、关键字保持英文。禁止半吊子英文。
- **代码修改时，必须同步修改相关注释**（尤其参数、返回值、异常说明），避免过期注释误导。

### 特殊标记
- **TODO**：待实现功能，格式 `// TODO(姓名/工号): 说明 [预计处理时间或版本]`。
- **FIXME**：已知错误或缺陷需修复，格式 `// FIXME(姓名/工号): 说明 [预计处理时间]`。
- 上线前应清理或落实 TODO/FIXME，避免遗留到生产。

### 禁止
- 禁止用注释保留大段废弃代码；若需保留须说明原因并收敛到少量行。
- 禁止无信息量的注释（如 `// 获取用户` 对 `getUser()`）；命名与结构应自解释，注释只补充必要信息。

## 空指针与集合
- 字面量在前：`"admin".equals(user.getName())`，禁止 `user.getName().equals("admin")`。
- 泛型必写：`List<String> list = new ArrayList<>();`，禁止原始 `List list`。
- 查询单条可选值推荐使用 `Optional`。
