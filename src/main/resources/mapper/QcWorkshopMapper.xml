<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zioneer.robotqcsystem.mapper.QcWorkshopMapper">

    <resultMap id="BaseResultMap" type="com.zioneer.robotqcsystem.domain.entity.QcWorkshop">
        <id column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="location" property="location"/>
        <result column="enabled" property="enabled"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">code, name, location, enabled, created_at, updated_at</sql>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM qc_workshop ORDER BY code
    </select>

    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM qc_workshop WHERE code = #{code}
    </select>

    <insert id="insert">
        INSERT INTO qc_workshop (code, name, location, enabled, created_at, updated_at)
        VALUES (#{code}, #{name}, #{location}, #{enabled}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updateByCode">
        UPDATE qc_workshop
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="location != null">location = #{location},</if>
            <if test="enabled != null">enabled = #{enabled},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        WHERE code = #{code}
    </update>

    <delete id="deleteByCode">
        DELETE FROM qc_workshop WHERE code = #{code}
    </delete>
</mapper>
