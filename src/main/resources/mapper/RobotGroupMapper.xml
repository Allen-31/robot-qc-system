<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zioneer.robotqcsystem.mapper.RobotGroupMapper">

    <resultMap id="BaseResultMap" type="com.zioneer.robotqcsystem.domain.entity.RobotGroup">
        <id column="id" property="id"/>
        <result column="group_no" property="groupNo"/>
        <result column="group_name" property="groupName"/>
        <result column="description" property="description"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, group_no, group_name, description, created_at, updated_at
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM robot_group
        WHERE id = #{id}
    </select>

    <select id="selectByGroupNo" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM robot_group
        WHERE group_no = #{groupNo}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM robot_group
        <where>
            <if test="keyword != null and keyword != ''">
                AND (group_no LIKE '%' || #{keyword} || '%'
                OR group_name LIKE '%' || #{keyword} || '%')
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countList" resultType="long">
        SELECT COUNT(1)
        FROM robot_group
        <where>
            <if test="keyword != null and keyword != ''">
                AND (group_no LIKE '%' || #{keyword} || '%'
                OR group_name LIKE '%' || #{keyword} || '%')
            </if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO robot_group (group_no, group_name, description, created_at, updated_at)
        VALUES (#{groupNo}, #{groupName}, #{description}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="updateById">
        UPDATE robot_group
        <set>
            <if test="groupName != null">group_name = #{groupName},</if>
            <if test="description != null">description = #{description},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM robot_group WHERE id = #{id}
    </delete>
</mapper>
